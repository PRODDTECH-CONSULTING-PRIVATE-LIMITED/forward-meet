import React from 'react';

// ParticipantCard is retained for backward-compatibility with App.jsx import.
// LocationSelector now handles its own rendering internally.
const ParticipantCard = ({ children }) => {
  return <>{children}</>;
};

export default ParticipantCard;
